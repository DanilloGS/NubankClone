import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    Container: {
        alignItems: 'center',
        marginTop: '15%',
    },
    Top: {
        flexDirection: 'row',
        alignItems:"center",
    },
    Title:{
        fontSize: 18,
        color :'#FFF',
        fontWeight: "bold",
        marginLeft: 8
    }
});

export default styles;