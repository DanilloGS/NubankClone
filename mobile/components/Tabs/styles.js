import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    Container: {
        height: 100,
        marginBottom: 20
    },
    TabContainer: {
        paddingLeft: 10,
        paddingRight: 20,
    }
});

export default styles;